{
  "name": "socket.io",
  "version": "1.0.0",
  "description": "This is a simple application built using **WebSockets** and **Socket.IO** to demonstrate real-time communication between the client and server. The project helps in understanding the basics of WebSockets, the **Socket.IO** library, and how to use them for building real-time applications.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0",
    "socket.io": "^4.8.1"
  }
}
